VALUE="inserted"
REPLACE=${VALUE}
REPLACE_SINGLE='${VALUE}'
REPLACE_DOUBLE="${VALUE}"
REPLACE_PARTIAL="partialy ${VALUE} value"
REPLACE_ESCAPED="partialy \${VALUE} value"
REPLACE_FROM_BASIC="${HASH_WITH_COMMENT}"
REPLACE_FROM_BROKEN="${EMPTY}"
